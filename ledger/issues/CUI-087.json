{
  "id": "CUI-087",
  "title": "Nothing checks that the commit a published package pins is one a reader can fetch",
  "type": "defect",
  "severity": "med",
  "status": "closed",
  "foundBy": "session agent, from a controlled test in which two agents were each given an installed copy of the package and asked how they would learn to use it",
  "opened": "2026-09-14",
  "resolved": "2026-09-14",
  "stint": "consumer-facing fixes, 2026-09-14",
  "evidence": {
    "commits": [
      "86824db"
    ],
    "integrated": [],
    "verified": "The check was demonstrated against the condition this record describes rather than against a fixture. `node scripts/stage-package.mjs` on this branch exits 1 and names the commit: it is on no remote branch and no tag, so it is not one a reader can fetch. That is the state the reported 404s came from.\n\nIt does not misfire on a published commit. Run against the commit `v0.3.0` points at, `assertCommitIsFetchable` accepts it, so `release.yml` and `npm.yml` keep publishing. Both fetch the full history and both stage from a commit that is on a pushed branch or a tag.\n\n`npm run test:stage-package` passes 13 tests, 3 of them new, with the git runner injected so they ask about the logic rather than about this checkout: a contained commit is accepted, an uncontained one is refused with the sha in the message, and a checkout with no ref to judge against is left alone.\n\n`npm run ci` exits 0 at 86824db, with the visual gate comparing 112 of 112 captures at 0 differing pixels, and the warning visible in its log."
  },
  "resolution": null,
  "details": "Every documentation pointer in the published package is an absolute URL pinned to the commit the build came from. `scripts/build.mjs` records that commit as whatever `git rev-parse HEAD` returns. Nothing then checks that the commit is one a reader can reach.\n\nSo a package staged from a commit that has not been pushed ships with its entire documentation surface dead. That is the README's seven links, the `spec` of all 49 catalog entries, and the manifest's `homepage`. Each returns 404, and nothing in the publish path reports it.\n\nObserved rather than reasoned about. A package was staged from an unmerged branch and given to an agent with no knowledge of this repository. It followed a catalog `spec` to the pinned URL, got 404, then asked the forge directly and was told `No commit found for SHA`. It recovered by reading the default branch instead, which is the outcome the pinning exists to prevent.\n\nThe workflows are safe. `npm.yml` runs on a push to the default branch and `release.yml` on a pushed tag, so both pin a commit the forge already has. The exposure is the manual path, which this repository documents: the root manifest's `prepublishOnly` refuses a direct publish and prints `npm run stage && npm publish .package` as the way to do it. Neither half of that command looks at whether the commit is public.\n\n`scripts/stage-package.mjs` already refuses to stage when `dist/BUILD.json` is missing or records no sha. It treats an absent commit as fatal and an unreachable one as fine, and the second ships a package that looks correct and documents nothing.",
  "notes": [
    {
      "date": "2026-09-14",
      "text": "The same check would have to decide what reachable means, and the honest answer is narrow. Asking the forge costs a network call in a script that otherwise needs none. Asking git whether the commit is an ancestor of a tracked remote branch is local and catches the case that actually happens, which is staging from work that has not left the machine."
    },
    {
      "date": "2026-09-14",
      "text": "The first attempt made the gate unusable. `scripts/ci.mjs` stages the package as one of its steps, so refusing an unpushed commit meant no branch could pass its own gate until it had been pushed. That is the wrong way round, because the gate exists to be run before anything leaves.\n\nStaging is now strict by default and reports rather than refuses when the caller says it is only inspecting the tarball. The gate says so, since it publishes nothing. A hand-run `npm run stage`, which is the path this record is about and the one `prepublishOnly` prints, says nothing and is refused."
    },
    {
      "date": "2026-09-14",
      "text": "Reachability is asked of this repository rather than of the forge. That costs no network in a script that otherwise needs none, and it answers the case that happens, which is staging from work that never left the machine. It is not proof of publication: a local tag would satisfy it, and a remote-tracking ref can be stale. Where there is no remote-tracking ref and no tag at all it says nothing, because a check with no evidence should not be what stops a release."
    }
  ],
  "links": [
    "CUI-086"
  ]
}
