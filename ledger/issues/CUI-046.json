{
  "id": "CUI-046",
  "title": "MarkdownViewer's specification has drifted from the component in four places",
  "type": "defect",
  "severity": "med",
  "status": "open",
  "foundBy": "an independent reviewer agent, one of three asked whether each component specification still describes the component that exists",
  "opened": "2026-09-06",
  "resolved": null,
  "stint": null,
  "evidence": {
    "commits": [],
    "integrated": [],
    "verified": null
  },
  "resolution": null,
  "details": "- The props table at `components/MarkdownViewer.md:31` lists fifteen props and omits three the component accepts: `onImageSrc`, `onHeadingNavigate` and `initialHeading`, declared at `src/components/MarkdownViewer.tsx:57`, `:60` and `:61`. Three shipped capabilities are invisible to a reader.\n- `:106` says a pane and its rail are never in the DOM together. `MarkdownViewer.tsx:777` renders the minimap whenever `minimap` is set, styled to zero width, while `:764` renders the rail beside it when collapsed.\n- `:91` describes the outline collapsing by transitioning its width to zero. `MarkdownViewer.tsx:684` unmounts it instead. The width transition it describes is what the minimap does, so the two descriptions are swapped.\n- `:185` says empty content renders an empty article. `MarkdownViewer.tsx:258` returns an empty-state block before any article, which the same document describes correctly at `:55`.\n\nThe second one matters most. The specification invites querying `[data-part=\"minimap\"]` as proof the pane is showing, and that selector matches while it is collapsed.",
  "notes": [
    {
      "date": "2026-09-06",
      "text": "Session agent confirmed every citation in this record against the tree at a374f2c before filing it."
    }
  ],
  "links": []
}
