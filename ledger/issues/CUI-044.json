{
  "id": "CUI-044",
  "title": "Component specifications describe styling no stylesheet applies",
  "type": "defect",
  "severity": "med",
  "status": "closed",
  "foundBy": "an independent reviewer agent, one of three asked whether each component specification still describes the component that exists",
  "opened": "2026-09-06",
  "resolved": "2026-09-07",
  "stint": "Table pass, 2026-09-07",
  "evidence": {
    "commits": [
      "bfc6163",
      "92ce7e2",
      "f866878",
      "6f2c943"
    ],
    "integrated": [],
    "verified": "All six bullets checked against the source, and the last two carry tests. Reverting the two Table rules fails both assertions in `src/components/Table.surface.test.tsx`, which reads the wrapper's computed background against `--card` and finds the hover rule that reaches a sortable header. `npm run ci` exits 0 with every gate run, visual:compare 104/104 at 0 differing pixels, axe 0."
  },
  "resolution": null,
  "details": "Six specifications document a visual treatment that no rule under `src/styles/` produces. Each was checked against the component's own sheet.\n\n- `components/Table.md:155` promises sortable headers turn `var(--color-accent)` on hover. `src/styles/components/table.css` has one `:hover` rule, at `:233`, and it is for rows.\n- `components/Table.md:100` gives the table `background: var(--card)`. `var(--card)` does not appear in `table.css`, so the table takes whatever is behind it.\n- `components/StatusBadge.md:60` gives the label `var(--font-weight-medium)`. The label carries `text-label-upper`, which is `var(--font-weight-semibold)` at `src/styles/utilities.css:4`.\n- `components/Panel.md:57` pins the title at `11px`. It resolves through `--font-size-label` to 12px, and `DESIGN_SYSTEM_SPEC.md:614` agrees with the code.\n- `components/Input.md:98` and `components/SearchInput.md:81` give the placeholder `var(--muted)`. Both sheets use `var(--color-neutral)`, which differs in both themes.\n- `components/SplitPane.md:64` describes the hit area as a `::before` extending 2px on each side. `src/styles/components/split-pane.css` has no `::before`, and the real child element is offset to one side instead.\n\nEach of these misleads someone matching their own markup to the library's.",
  "notes": [
    {
      "date": "2026-09-06",
      "text": "Session agent confirmed every citation in this record against the tree at a374f2c before filing it."
    },
    {
      "date": "2026-09-06",
      "text": "The SplitPane bullet is done, in bfc6163. `components/SplitPane.md` now describes the hit area as the child element it is, `0.5rem` wide and offset `-0.25rem`, rather than a `::before` extending evenly on both sides. The Table, StatusBadge, Panel and placeholder bullets are still open."
    },
    {
      "date": "2026-09-06",
      "text": "The placeholder bullet needs a decision and was left alone during the Input pass. `components/Input.md:98` and `components/SearchInput.md:81` say `var(--muted)`, and both sheets use `var(--color-neutral)`. Changing the sheets repaints every placeholder in both themes and moves the baseline. Changing the documents blesses the colour that ships today. Neither is obviously right, so it is for the repository owner rather than a session agent."
    },
    {
      "date": "2026-09-06",
      "text": "The placeholder bullet is done, in 92ce7e2. The owner chose to document the colour that ships rather than repaint every placeholder, so `components/Input.md` and `components/SearchInput.md` now name `var(--color-neutral)`. The Table, StatusBadge and Panel bullets are still open."
    },
    {
      "date": "2026-09-06",
      "text": "The StatusBadge and Panel bullets are done, in f866878, on the same principle the owner set for the placeholder: name what ships. StatusBadge is semibold through the `text-label-upper` utility, and Panel's title is `--font-size-label` at 0.75rem rather than a hard-coded 11px.\n\nThe two Table bullets are left open, and they are a different shape. Both are absences rather than wrong values: no `:hover` rule for a sortable header, and no `var(--card)` background. Naming what ships would mean deleting two documented affordances, and adding them would repaint every table. Either way it is a product decision rather than a correction."
    },
    {
      "date": "2026-09-07",
      "text": "The Table bullets closed on measurement that corrected the advice given with them. The background was presented as repainting three tables from the page grey to the card colour. It repaints nothing: toggling the rule on the rendered table changes 0 of its 976 by 381 pixels, because the header row paints `--color-bg-muted` and the body rows cover the rest, so the wrapper's own background is never exposed. The earlier figure came from measuring what sits behind the table rather than what is visible through it.\n\nThe owner kept it on the narrow argument: the wrapper already draws a border and a radius, so it is a surface, and one relying on its children to stay opaque is fragile where consumers restyle rows. The hover is not inert. A sortable header turns `var(--color-accent)` under a pointer, which no capture can photograph, so the test asserts the rule's reach instead."
    },
    {
      "date": "2026-09-07",
      "text": "Correction to the note above: the card background is not inert. It repaints the table body from the page grey to `var(--card)`, measured at the pixel level as 243,244,246 becoming 255,255,255 across 358109 of `table.png`'s 371856 pixels. The earlier claim of 0 differing pixels came from running pixelmatch at the gate's own threshold, which tolerates a shift of that size, and reading its answer as equality. The reasoning for keeping the rule stands; the description of what it does was wrong."
    }
  ],
  "links": []
}
