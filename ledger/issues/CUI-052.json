{
  "id": "CUI-052",
  "title": "Re-recording the baseline rewrites captures whose pixels did not change",
  "type": "defect",
  "severity": "low",
  "status": "open",
  "foundBy": "session agent, while re-recording the baseline for CUI-040",
  "opened": "2026-09-06",
  "resolved": null,
  "stint": null,
  "evidence": {
    "commits": [],
    "integrated": [],
    "verified": null
  },
  "resolution": null,
  "details": "`npm run visual:capture` rewrote 17 of the 94 captures for a change that affected two components. Measured against the committed versions, `code-block` changed 1.82% and 1.79% of its pixels and `mermaid-diagram` 0.53% and 0.64%. The other 13 differed by 0 pixels at the threshold the gate itself uses.\n\nRestoring those 13 leaves `visual:compare` at 94 of 94 with 0 differing pixels, so they carry nothing the gate can see. Running `visual:capture` again rewrites all 17 once more, so this is reproducible rather than one bad run.\n\nThe cost is review. CONTRIBUTING asks that an intentional visual change be reviewed in both themes, and 13 files of noise around 4 real ones makes that harder than it needs to be. Writing a capture only when it differs from the committed one would fix it.\n\nWhat has not been established is why the bytes differ when the pixels do not. It could be encoder nondeterminism or a colour delta below the comparison threshold, and the two want different fixes.",
  "notes": [
    {
      "date": "2026-09-07",
      "text": "The cause in `details` is wrong, and so is the word churn. There is no nondeterminism: two captures of the same commit produce 104 byte-identical images, measured across every file in both themes. Every capture the recorder rewrites is a change the code actually makes."
    },
    {
      "date": "2026-09-07",
      "text": "What is really happening is that the comparison tolerates changes the recorder records, and by a wide margin. `visual:compare` reported 0 differing pixels for a `table.png` in which 358109 of 371856 pixels had moved about 12 per channel, the whole table body repainting from the page grey to its own card background. pixelmatch weighs a perceptual distance against a threshold of 0.1, roughly a tenth of the maximum, and a shift of 12 in 255 sits under it however many pixels it covers.\n\nSo the gate is blind to a uniform colour change of that size. A token repointed, a surface recoloured or a theme drifting would pass. That is a bigger question than the diff noise this record was filed about, and it is the one worth answering."
    },
    {
      "date": "2026-09-07",
      "text": "This also cost something. Twice in this session the zero-diff files from a re-record were restored on the reasoning that they carried no change, which discarded real renderings and left the committed baseline disagreeing with the code. Twenty captures were stale by the time it was measured, and are re-recorded in 348e80e. Reading a tolerant comparison as proof of equality is what caused it, and it is the same mistake in both directions: it hid the drift and it hid the Table background's effect."
    }
  ],
  "links": [
    "CUI-040"
  ]
}
