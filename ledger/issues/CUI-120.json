{
  "id": "CUI-120",
  "title": "A push that changes only the ledger runs ci and republishes an unchanged build",
  "type": "improvement",
  "severity": "low",
  "status": "closed",
  "foundBy": "user report (host): \"support ledger builds on pushes that only contain ledger changes, with just linting, ledger checking, and a pages build to publish the ledger page\"",
  "opened": "2026-09-23",
  "resolved": "2026-09-23",
  "stint": "ledger-builds",
  "evidence": {
    "commits": [
      "d8a36fe"
    ],
    "integrated": [],
    "verified": "Pushing 1178b87, which changed only `ledger/`, started the ledger and pages workflows and no ci. Both passed, and the status file still lists d8a36fe first in `built`. With no ci, npm and publish images did not run either."
  },
  "resolution": "ci skips a push that changes only `ledger/`. The ledger workflow runs the ledger check and the document linters, and pages republishes the ledger's page. Nothing is built or published, so `built` stays.",
  "details": "Closing an issue is a commit that changes only `ledger/`, and pushing it runs the whole of `ci`. `ci` finishing then publishes a build of code that has not changed: its npm package and its image. Such a push needs only the ledger check, the document linters and a site build, which publishes the ledger's page.",
  "notes": [],
  "links": []
}
