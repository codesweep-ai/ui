{
  "id": "CUI-004",
  "title": "SectionedTree forces a filter box and an expand-all control on every section",
  "type": "defect",
  "severity": "med",
  "status": "closed",
  "foundBy": "an agent building a six-section navigation sidebar against 0.2.0, relayed by the repository owner",
  "opened": "2026-09-06",
  "resolved": "2026-09-06",
  "stint": "add-ledger, 2026-09-06",
  "evidence": {
    "commits": [
      "5d4f99d"
    ],
    "integrated": [],
    "verified": "npx vitest run src/components/SectionedTree.chrome.test.tsx: 6 passed, covering the default, turning every filter off, one section overriding the rest, and dropping the expand-all control. Full suite 606 passed across 60 files, up from 554 across 56 at the start of this work. npm run check exit 0. npm run visual:compare 76/76 screenshots passed, 161 differing pixels against a 0.1% threshold, axe violations 0 in both themes and no rule matching more nodes than the baseline."
  },
  "resolution": null,
  "details": "`SectionedTree` renders a filter input and an expand-all control for every section, and neither can be turned off. It sets `filterable`, `onToggleExpandAll` and `allExpanded` on each child Tree itself, and exposes none of them on its own props.\n\nWith six sections that is six filter boxes and six expand-all controls, most of them over sections of two to eleven items. In a 300px sidebar the chrome outweighed the content.\n\nThe inconsistency is the argument for calling this a defect. Plain `Tree` offers `filterable` as opt-in. `SectionedTree` composes Trees and removes a choice the composed component gives.\n\nWorked around in the consuming app by abandoning SectionedTree for Panel plus Tree per section, with a filter only on sections above twenty items. That took six filter boxes down to two.",
  "notes": [
    {
      "date": "2026-09-06",
      "text": "Confirmed: src/components/SectionedTree.tsx:311-315 passes filterable, onToggleExpandAll and allExpanded unconditionally. SectionedTreeProps at lines 26-48 exposes none of the three."
    },
    {
      "date": "2026-09-06",
      "text": "Fixed as filterable and expandAllControl, both defaulting to true so nothing existing changes, with TreeSection.filterable overriding per section. If you abandoned SectionedTree for Panel plus Tree because of this, it is worth another look: filterable={false} with a per-section override is what the workaround was reproducing by hand."
    }
  ],
  "links": [
    "CUI-003",
    "CUI-011"
  ]
}
