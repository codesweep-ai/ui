{
  "id": "CUI-010",
  "title": "MarkdownViewer outline and minimap collapse state cannot be observed",
  "type": "improvement",
  "severity": "med",
  "status": "open",
  "foundBy": "an agent building a documentation browser against 0.2.0, relayed by the repository owner",
  "opened": "2026-09-06",
  "resolved": null,
  "stint": null,
  "evidence": {
    "commits": [],
    "integrated": [],
    "verified": null
  },
  "resolution": null,
  "details": "`outlineCollapsed` and `minimapCollapsed` seed internal state and are never read again. There is no change callback, so a consumer cannot learn that the user collapsed a pane.\n\nThe cost is larger than it sounds. A viewer is typically keyed per document, so it remounts on every document switch and both panes reset. The user's choice is lost on each navigation, not merely on reload.\n\nWith no callback the only route open to the consumer was a capture-phase click listener matching the component's own aria-labels, against the pattern `/^(Collapse|Expand) (outline|minimap)$/`. That reaches into the component's accessible names, which are not an API, and it breaks the moment a label is reworded.\n\nThe proposal is `onOutlineCollapsedChange` and `onMinimapCollapsedChange`, or an internal `storageKey` that persists the state.\n\nWorked around in the consuming app with exactly that aria-label listener.",
  "notes": [
    {
      "date": "2026-09-06",
      "text": "Confirmed: src/components/MarkdownViewer.tsx:283-287 seeds useState from the props and never syncs back. No onOutlineCollapsedChange or onMinimapCollapsedChange exists in the file."
    },
    {
      "date": "2026-09-07",
      "text": "The repository owner adopted the stopping rule this branch had proposed and never had confirmed: fix the defects a consumer would hit, record everything else, and decide the rest at 0.3.0. This record is one of the six that wait, because each adds or changes a public prop, and `CONTRIBUTING.md` asks that the design be settled before the code is written.\n\nThey are one conversation rather than six, and this is the order they unlock in: CUI-014 first, because whether navigation is a supported use decides CUI-011; then CUI-012, because the `storageKey` convention decides CUI-010; then CUI-009 and CUI-011 together, which are both about sizing to content; and CUI-013 last, which stands alone. The Tooltip and MarkdownMinimap changes that already landed belong in the same conversation.\n\nUntil it is settled, the consuming app keeps the capture-phase listener matching the component's aria-labels, which breaks on a wording change rather than on a version bump."
    },
    {
      "date": "2026-09-07",
      "text": "CUI-012 is settled, which narrows this. Convention 7.3 now says user-arrangement state belongs behind a `storageKey`, and a collapsed pane is arrangement, so `storageKey` is the convention-consistent answer rather than one of two options.\n\nThat does not close this record. A consumer keying the viewer per document still wants to know when a pane collapsed, and a `storageKey` alone does not tell it. Whether the callbacks ship alongside is the part still to decide."
    }
  ],
  "links": [
    "CUI-012"
  ]
}
