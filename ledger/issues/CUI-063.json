{
  "id": "CUI-063",
  "title": "A tree's expand-all control renders only when `filterable` is on, and nothing says so",
  "type": "defect",
  "severity": "med",
  "status": "open",
  "foundBy": "session agent, while giving the two expand-all controls names that differ in CUI-060",
  "opened": "2026-09-08",
  "resolved": null,
  "stint": null,
  "evidence": {
    "commits": [],
    "integrated": [],
    "verified": null
  },
  "resolution": null,
  "details": "`Tree` renders its expand-all control inside the toolbar that `filterable` gates, and `filterable` defaults to `false`. So passing `onToggleExpandAll` to a `Tree` renders nothing, and nothing says why.\n\n`docs-compile` found it. `components/Tree.md` documents `data-part=\"expand-all\"`, and that gate holds every documented hook against the specification's own compiling example. The example passed `onToggleExpandAll` and rendered no control, which is the same silence a consumer meets.\n\n`SectionedTree` hides the coupling rather than fixing it. Its `filterable` defaults to `true`, so the controls appear until a consumer turns the filter off, and then the per-section expand-all controls go with it. A navigation sidebar turning off a filter box it does not want loses a control it never asked to lose.\n\nNeither specification mentions the dependency. A prop named for a filter box should not decide whether an unrelated control renders, so the fix is to give the control its own condition rather than to document the coupling.",
  "notes": [],
  "links": [
    "CUI-060"
  ]
}
